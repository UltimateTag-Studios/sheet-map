/** Invisible circle tap target for map dots (markers, path points, user location). */
export const MAP_DOT_HIT_RADIUS_PX = 16;

export const mapDotHitLayerPaint = {
  "circle-radius": MAP_DOT_HIT_RADIUS_PX,
  "circle-opacity": 0,
} as const;
