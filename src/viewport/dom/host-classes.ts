/** Class on the map + sheet sizing root (`SheetHost`). */
export const SHEET_HOST_CLASS = "sheet-host";

/** Class on the bottom-anchored sheet slide element. */
export const SHEET_SLIDE_CLASS = "sheet-slide";
