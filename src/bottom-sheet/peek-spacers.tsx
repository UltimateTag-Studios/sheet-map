/** Mirrors handle block below user peek for vertical balance — always in open sheet peek. */
export function SheetMapHandleSpacer() {
  return <div aria-hidden className="sheet-map-handle-spacer shrink-0" />;
}
